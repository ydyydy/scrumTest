export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum PaginationDefaults {
  DEFAULT_PAGE = 1,
  DEFAULT_LIMIT = 10,
  MAX_LIMIT = 100,
}

export enum QuestionCategory {
  FUNDAMENTOS_PRINCIPIOS = 1,
  ROLES_EQUIPOS = 2,
  ARTEFACTOS = 3,
  EVENTOS_RITMOS = 4,
  ESTIMACION_METRICAS = 5,
  HERRAMIENTAS_TECNICAS = 6,
}
